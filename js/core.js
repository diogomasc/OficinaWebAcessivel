/**
 * Arquivo principal que inicializa todos os módulos de acessibilidade
 * Este script atua como centralizador das funcionalidades
 */

// Aguarda o carregamento completo do DOM antes de executar o código
document.addEventListener("DOMContentLoaded", function () {
  // Este arquivo só contém a inicialização dos módulos
  // As funcionalidades estão distribuídas em arquivos separados

  // A separação em módulos facilita a manutenção e compreensão do código
  console.log("Módulos de acessibilidade carregados");
});
